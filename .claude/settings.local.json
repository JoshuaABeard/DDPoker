{
  "permissions": {
    "allow": [
      "Bash(git clone:*)",
      "Bash(dir \"C:\\\\Repos\\\\DDPoker\")",
      "Bash(dir:*)",
      "Bash(git init:*)",
      "Bash(git remote add:*)",
      "Bash(git fetch:*)",
      "Bash(git checkout:*)",
      "Bash(docker search:*)",
      "Bash(winget search:*)",
      "Bash(curl:*)",
      "Bash(powershell -Command \"Expand-Archive -Path ''C:\\\\Repos\\\\maven.zip'' -DestinationPath ''C:\\\\Program Files'' -Force\")",
      "Bash(powershell -Command:*)",
      "Bash(\"C:\\\\Program Files\\\\Eclipse Adoptium\\\\jdk-25.0.2.10-hotspot\\\\bin\\\\java.exe\" -version)",
      "Bash(export JAVA_HOME=\"C:\\\\Program Files\\\\Eclipse Adoptium\\\\jdk-25.0.2.10-hotspot\")",
      "Bash(export PATH=\"/c/Program Files/Eclipse Adoptium/jdk-25.0.2.10-hotspot/bin:/c/Tools/apache-maven-3.9.12/bin:$PATH\":*)",
      "Bash(java:*)",
      "Bash(mvn:*)",
      "Bash(winget install:*)",
      "Bash(find:*)",
      "Bash(export PATH=\"/c/Tools/apache-maven-3.9.12/bin:$PATH\")",
      "Bash(export JAVA_HOME=\"/c/Program Files/Eclipse Adoptium/jdk-25.0.1.1-hotspot\")",
      "Bash(where:*)",
      "Bash(export JAVA_HOME=\"/c/Program Files/Eclipse Adoptium/jdk-25.0.2.10-hotspot\")",
      "Bash(export PATH=\"/c/Tools/apache-maven-3.9.12/bin:$JAVA_HOME/bin:$PATH\")",
      "Bash(ls \"C:\\\\Repos\\\\DDPoker\\\\code\\\\pokerserver\\\\target\\\\dependency\"\" | wc -l && ls \"C:ReposDDPokercodepokerwickettargetdependency\"\")",
      "Bash(docker compose build:*)",
      "Bash(export PATH=\"/c/Program Files/Docker/Docker/resources/bin:$PATH\")",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git push:*)",
      "Bash(grep:*)",
      "Bash(for file in PokerGame.java PokerUDPServer.java PokerDatabase.java PokerStartMenu.java ShowTournamentTable.java online/ChatPanel.java dashboard/ObserversDash.java)",
      "Bash(do sed -i 's/\\\\.getKey\\(\\)/\\\\.getPlayerId\\(\\)/g' \"$file\")",
      "Bash(echo:*)",
      "Bash(done)",
      "Bash(do sed -i 's/\\\\.setKey\\(/\\\\.setPlayerId\\(/g' \"$file\")",
      "Bash(tee:*)",
      "Bash(javap:*)",
      "Bash(ls:*)",
      "Bash(test:*)",
      "Bash(xargs git show:*)",
      "WebFetch(domain:www.ddpoker.com)",
      "Bash(xargs:*)",
      "Bash(for dir in \"C:\\\\Repos\\\\DDPoker\\\\code\"/*)",
      "Bash(do if [ -d \"$dir/src/test/java\" ])",
      "WebFetch(domain:github.panteleyev.org)",
      "WebFetch(domain:github.com)",
      "WebFetch(domain:akman.github.io)",
      "Bash(pwsh:*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"try { if ($env:TELEGRAM_BOT_TOKEN -and $env:TELEGRAM_CHAT_ID) { $claudeDir = Join-Path $env:USERPROFILE '.claude'; if (-not (Test-Path $claudeDir)) { New-Item -ItemType Directory -Path $claudeDir -Force | Out-Null }; $startFile = Join-Path $claudeDir 'session_start.tmp'; $startTime = [DateTimeOffset]::Now.ToUnixTimeSeconds(); Set-Content -Path $startFile -Value $startTime -NoNewline -Force; $projectDir = 'DDPoker'; try { $projectDir = Split-Path -Leaf (Get-Location) } catch {}; $now = Get-Date; $message = '🚀 Claude Code Session Started - Project: ' + $projectDir + ' - Time: ' + $now.ToString('HH:mm:ss'); try { Invoke-RestMethod -Uri \\\"https://api.telegram.org/bot$env:TELEGRAM_BOT_TOKEN/sendMessage\\\" -Method Post -Body @{chat_id=$env:TELEGRAM_CHAT_ID; text=$message} -ErrorAction SilentlyContinue | Out-Null } catch {} } } catch {}; exit 0\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"try { if ($env:TELEGRAM_BOT_TOKEN -and $env:TELEGRAM_CHAT_ID) { $claudeDir = Join-Path $env:USERPROFILE '.claude'; $startFile = Join-Path $claudeDir 'session_start.tmp'; $durationText = 'Unknown'; if (Test-Path $startFile) { try { $startTime = [int64](Get-Content $startFile -Raw); $endTime = [DateTimeOffset]::Now.ToUnixTimeSeconds(); $duration = $endTime - $startTime; $minutes = [math]::Floor($duration / 60); $seconds = $duration % 60; $durationText = \\\"${minutes}m ${seconds}s\\\" } catch { $durationText = 'Error' }; Remove-Item $startFile -Force -ErrorAction SilentlyContinue }; $projectDir = 'DDPoker'; try { $projectDir = Split-Path -Leaf (Get-Location) } catch {}; $now = Get-Date; $message = '✅ Claude Code Session Completed - Project: ' + $projectDir + ' - Duration: ' + $durationText + ' - Time: ' + $now.ToString('HH:mm:ss'); try { Invoke-RestMethod -Uri \\\"https://api.telegram.org/bot$env:TELEGRAM_BOT_TOKEN/sendMessage\\\" -Method Post -Body @{chat_id=$env:TELEGRAM_CHAT_ID; text=$message} -ErrorAction SilentlyContinue | Out-Null } catch {} } } catch {}; exit 0\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"try { if ($env:TELEGRAM_BOT_TOKEN -and $env:TELEGRAM_CHAT_ID) { $claudeDir = Join-Path $env:USERPROFILE '.claude'; if (-not (Test-Path $claudeDir)) { New-Item -ItemType Directory -Path $claudeDir -Force | Out-Null }; $bashStartFile = Join-Path $claudeDir 'bash_start.tmp'; $timestamp = [DateTimeOffset]::Now.ToUnixTimeSeconds(); Set-Content -Path $bashStartFile -Value $timestamp -NoNewline -Force } } catch {}; exit 0\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"try { if ($env:TELEGRAM_BOT_TOKEN -and $env:TELEGRAM_CHAT_ID) { $claudeDir = Join-Path $env:USERPROFILE '.claude'; $startFile = Join-Path $claudeDir 'bash_start.tmp'; if (Test-Path $startFile) { try { $startTime = [int64](Get-Content $startFile -Raw); $endTime = [DateTimeOffset]::Now.ToUnixTimeSeconds(); $duration = $endTime - $startTime; Remove-Item $startFile -Force -ErrorAction SilentlyContinue; if ($duration -gt 300) { $minutes = [math]::Floor($duration / 60); $seconds = $duration % 60; $projectDir = 'DDPoker'; try { $projectDir = Split-Path -Leaf (Get-Location) } catch {}; $now = Get-Date; $message = '⚠️ Long Bash Operation - Duration: ' + $minutes + 'm ' + $seconds + 's - Project: ' + $projectDir + ' - Time: ' + $now.ToString('HH:mm:ss'); try { Invoke-RestMethod -Uri \\\"https://api.telegram.org/bot$env:TELEGRAM_BOT_TOKEN/sendMessage\\\" -Method Post -Body @{chat_id=$env:TELEGRAM_CHAT_ID; text=$message} -ErrorAction SilentlyContinue | Out-Null } catch {} } } catch {} } } } catch {}; exit 0\""
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"try { if ($env:TELEGRAM_BOT_TOKEN -and $env:TELEGRAM_CHAT_ID) { $projectDir = 'DDPoker'; try { $projectDir = Split-Path -Leaf (Get-Location) } catch {}; $now = Get-Date; $message = '🔔 Claude Code Notification - Project: ' + $projectDir + ' - Time: ' + $now.ToString('HH:mm:ss') + ' - Status: Waiting for user input or permission'; try { Invoke-RestMethod -Uri \\\"https://api.telegram.org/bot$env:TELEGRAM_BOT_TOKEN/sendMessage\\\" -Method Post -Body @{chat_id=$env:TELEGRAM_CHAT_ID; text=$message} -ErrorAction SilentlyContinue | Out-Null } catch {} } } catch {}; exit 0\""
          }
        ]
      }
    ]
  }
}
