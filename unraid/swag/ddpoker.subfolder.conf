## Version 2026/02/11
## DD Poker subfolder reverse proxy configuration for SWAG
## Production configuration - points to DDPoker container only
## Use this if you prefer yourdomain.com/ddpoker instead of ddpoker.yourdomain.com
##
## INSTALLATION:
## 1. Copy this file to your SWAG config: /config/nginx/proxy-confs/ddpoker.subfolder.conf
## 2. Ensure DDPoker container is on the same Docker network as SWAG
## 3. Restart SWAG container

location /ddpoker {
    return 301 $scheme://$host/ddpoker/;
}

location ^~ /ddpoker/ {
    include /config/nginx/proxy.conf;
    include /config/nginx/resolver.conf;
    set $upstream_app DDPoker;
    set $upstream_port 8080;
    set $upstream_proto http;
    proxy_pass $upstream_proto://$upstream_app:$upstream_port/;
}
